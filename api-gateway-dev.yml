server:
  port: 8080

eureka:
  instance:
    prefer-ip-address: true
    non-secure-port: 8099
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/

management:
  security:
    enabled: false
